import React from 'react';
import PageContent from '../components/PageContent';

const WorksPage = ({ transition }) => <PageContent style={transition && transition.style}>Works Page</PageContent>;

export default WorksPage;
