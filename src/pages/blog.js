import React from 'react';
import PageContent from '../components/PageContent';

const BlogPage = ({ transition }) => <PageContent style={transition && transition.style}>Blog Page</PageContent>;

export default BlogPage;
